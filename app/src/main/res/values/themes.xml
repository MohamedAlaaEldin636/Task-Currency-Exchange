<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskCurrencyExchange" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
    </style>

    <style name="Theme.TaskCurrencyExchange" parent="Base.Theme.TaskCurrencyExchange" />

    <!-- After digging in Toolbar.setup with nav controller found out below is where the color is fetched isa. -->
    <style name="ToolbarDrawerArrowToggle" parent="@style/Base.Widget.AppCompat.DrawerArrowToggle">
        <item name="android:textColorSecondary">@color/white</item>
    </style>
</resources>